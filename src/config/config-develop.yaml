app:
  port: 8000
  name: belajar
  environment: develop
  key: randomkey
  jwt_key: randomkey


database:
  htds:
    host: "localhost"
    port: 5432
    name: "HTDS"
    username: "postgres"
    password: "password"
